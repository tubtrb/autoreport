# v0.4 Sandboxes

This directory holds recreated `incubator2` rehearsal runs.

Rules:

- use editorial or user-template contract-first flows only
- keep tracked inputs under each sandbox `inputs/` directory
- regenerate plans, contracts, drafts, artifacts, reviews, and logs locally
- do not copy weekly sandboxes from the older incubator

Current sandboxes:

- `demo-run-001`: built-in editorial workflow automation alpha rehearsal
- `demo-run-002`: mixed user-template workflow automation alpha rehearsal
